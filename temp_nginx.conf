server {
    listen 80 default_server;
    server_name _;
    
    # 1. Customer App (Root)
    root /var/www/html/staging.resortwala.com;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 2. Vendor App Assets (Strict handling)
    location /vendor/assets/ {
        alias /var/www/html/stagingvendor.resortwala.com/assets/;
        expires 1y;
        access_log off;
    }

    # 2. Vendor App Routing
    location /vendor {
        alias /var/www/html/stagingvendor.resortwala.com;
        try_files $uri $uri/ /vendor/index.html;
    }

    # 3. Admin App Assets (Strict handling)
    location /admin/assets/ {
        alias /var/www/html/stagingadmin.resortwala.com/assets/;
        expires 1y;
        access_log off;
    }

    # 3. Admin App Routing
    location /admin {
        alias /var/www/html/stagingadmin.resortwala.com;
        try_files $uri $uri/ /admin/index.html;
    }

    # 4. Storage (Shared/API)
    location /storage {
        alias /var/www/html/stagingapi.resortwala.com/public/storage;
        try_files $uri $uri/ =404;
    }

    # 4. API Proxy (High Priority)
    location ^~ /api {
        alias /var/www/html/stagingapi.resortwala.com/public;
        try_files $uri $uri/ @laravel;
    }

    location @laravel {
        rewrite /api/(.*)$ /api/$1 break;
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/stagingapi.resortwala.com/public/index.php;
    }
    include /etc/nginx/snippets/kaamcheck.conf;
}
